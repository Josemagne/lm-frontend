/**
 * Synchronizes the data on indexedDB with that of 
 */
export default function synchronize(): Promise<boolean> {

    // if metadata.notSynchronizedBooks has any values

    // Then Correct according to book.status (add, delete, update)

    // If success then remove the values from metadata.notSynchronizedBooks

    // Else return 0;

}
describe("GoalPage", () => {
  it("Should load the goals", () => {})

  it("Should create a goal", () => {})

  it("Should update a goal", () => {})

  it("Should delete a goal", () => {})

  context("GoalModal", () => {
    it("Should open the GoalModal if the button is clicked", () => {})
    it("Should close the GoalModal if the close button is clicked", () => {})
  })

  context("GoalAdder", () => {
    it("Should open the GoalAdder if the button is clicked", () => {})
    it("Should close the GoalAdder if the button is clicked", () => {})
  })

  context("GoalCalender", () => {})
})

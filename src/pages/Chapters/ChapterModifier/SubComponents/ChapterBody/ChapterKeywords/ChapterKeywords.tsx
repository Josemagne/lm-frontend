import React from "react";

type Props = {};

/**
 * Lets us add keywords for a chapter
 * @param props
 * @returns
 */
const ChapterKeywords = (props: Props) => {
  return <div className="lm-chaptermodifier__keywords">ChapterKeywords</div>;
};

export default ChapterKeywords;

import React from "react";

type Props = {};

/**
 * Page where we show the settings
 * @param props
 * @returns
 */
const SettingsPage = (props: Props) => {
  return <div>SettingsPage</div>;
};

export default SettingsPage;

import React from "react"

type Props = {}

/**
 *
 * @param props
 * @returns
 */
const VocabularyPage = (props: Props) => {
  return <div className="lm-vocabulary"></div>
}

export default VocabularyPage

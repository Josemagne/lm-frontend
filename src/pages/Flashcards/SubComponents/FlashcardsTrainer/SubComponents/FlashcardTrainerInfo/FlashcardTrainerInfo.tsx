import React from "react"

/**
 * Shows informatin about the component to the user
 * @returns
 */
const FlashcardTrainerInfo = () => {
  return <div className="flashcardtrainer__info">FlashcardTrainerInfo</div>
}

export default FlashcardTrainerInfo

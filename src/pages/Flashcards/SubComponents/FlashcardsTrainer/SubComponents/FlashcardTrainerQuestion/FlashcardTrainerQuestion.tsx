import React from "react"

type Props = {}

/**
 * Shows the question for the revision
 * @param props
 * @returns
 */
const FlashcardTrainerQuestion = (props: Props) => {
  return <div className="flashcardtrainer__question"></div>
}

export default FlashcardTrainerQuestion

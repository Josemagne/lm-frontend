import React from "react";

type Props = {};

/**
 * Filters the books
 * @param props
 * @returns
 */
const BooksFilter = (props: Props) => {
  return (
    <div>
      {/* genre */}
      {/* language */}
      {/* pages */}
      {/* author */}
      {/* read */}
    </div>
  );
};

export default BooksFilter;

import { screen, render } from "@testing-library/react";
import BookModal from "./BookModal";

describe("BookModal", () => {
  test("Should open modal", () => {
    // render(<BookModal selectedBook="" />)
    expect(true).toBeTruthy();
  });
});

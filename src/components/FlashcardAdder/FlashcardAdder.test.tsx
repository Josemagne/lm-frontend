import { render } from "@testing-library/react"
import FlashcardAdder from "./FlashcardAdder"

describe("FlashcardAdder", () => {
  it("Should", () => {
    render(<FlashcardAdder type="BOOK" />)
  })
})
